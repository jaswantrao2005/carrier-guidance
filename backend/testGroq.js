require(dotenv).config(); const Groq = require(groq-sdk); const groq = new Groq({ apiKey: process.env.GROQ_API_KEY }); async function testGroq() { try { const res = await groq.chat.completions.create({ messages: [{ role: user, content: Return JSON: {"status": "ok", "score": 85} }], model: llama-3.3-70b-versatile, response_format: { type: json_object } }); console.log(Groq Success:, res.choices[0]?.message?.content); } catch (err) { console.error(Groq Error:, err.message); } } testGroq();
